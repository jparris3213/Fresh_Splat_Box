<div class="menu-container">
  <div class="card-header col-md-12 text-center">
    <h2>
      &#127869 Formal List
    </h2>
  </div>

  <table>
    <tr>
      <th class="col-md-1">Level</th>
      <th class="col-md-4">Scroll Name</th>
      <th class="col-md-4">Component</th>
      <th class="col-md-2">Price</th>
      <th class="col-md-1">Shotted?</th>
    </tr>
    {{#each scrolls as |scroll|}}
    <tr>
      <td>{{scroll.level}}</td>
      <td>{{scroll.scroll_name}}</td>
      <td>{{scroll.component}}</td>
      <td>{{scroll.base_cost}} {{scroll.base_coin}}</td>
      <td>{{#if scroll.shot_status}} Yes {{else}} No {{/if}}</td>
    </tr>
    {{/each}}
      
  </table>












